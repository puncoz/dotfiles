# ~/.tmuxinator/vdrome.yml
name: vdrome
root: ~/work/noa/velodrome

pre: mkdir -p /tmp/images /tmp/velodrome
post: rm -rf /tmp/velodrome /tmp/images

startup_window: cli
windows:
  - code:
    - vim
  - cli:
    - #empty
  - server:
    - ./manage.py runserver
  - todo:
    - vim -c "Gist -l"
