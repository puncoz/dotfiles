# python
alias 2cv='virtualenv .venv'
alias 3cv='python -m venv .venv'
alias aav='echo ". .venv/bin/activate" >> .env && source .env'
alias av='source .env'
alias psv='source .venv/bin/activate'

# ipython
alias ipython='ipython --profile=lwm'
alias ipy='ipython'

# stack
alias stw='stack test --file-watch --fast'
alias sg='stack ghci'
alias sb='stack build'
alias whv='stack exec ghc-pkg latest'
alias gpk='stack exec ghc-pkg'
alias sld='stack list-dependencies'
alias ghc='stack exec -- ghc'
alias ghci='stack exec -- ghci'

# editor
alias v='nvim'
alias vi='nvim'
alias vim='nvim'
alias bim='nvim'

# networking
alias pnm='sudo pkill NetworkManager'
alias p8='ping 8.8.8.8'

# postgres
alias pgs='sudo -u postgres'
alias prs='sudo service postgresql restart'

# builtin
alias s='ls'
alias gR='grep -iR'
alias xclip='xclip -selection c'
alias -g L='| less'
alias -g SO='1> /dev/null'
alias -g SE='2> /dev/null'
alias cnt='wc -l'

# dotfiles
alias erm='$EDITOR ~/remind/reminders.txt'
alias ev='$EDITOR ~/.config/nvim/init.vim'
alias ez='$EDITOR ~/.zshrc'
alias ea='$EDITOR ~/.zsha'
alias ef='$EDITOR ~/.zshf'
alias et='$EDITOR ~/.tmux.conf'
alias en='$EDITOR ~/notes/TODO.md'
alias sz='source ~/.zshrc'
alias dotf='cd ~/dotfiles'

# apt
alias aptar='sudo apt -y autoremove'
alias aptup='sudo apt update'
alias aptug='sudo apt upgrade'
alias aptdg='sudo apt dist-upgrade'
alias aptr='sudo apt -y remove'
alias aptp='sudo apt -y purge'
alias apti='sudo apt -y install'
alias apts='apt-cache search'
alias aptsh='apt-cache show'

# git-rebase
alias grim='git rebase -i --root master'
alias grb='git rebase'
alias grm='git rebase master'
alias grc='git rebase --continue'
alias grA='git rebase --abort'
alias grq='git rebase -i --autosquash'

# git-reset
alias gres='git reset'
alias grH='git reset HEAD~'

# git-init
alias gi='git init'

# git-status
alias gs='git status -sb'
alias gdel='git status -s | cut -c4- | xargs rm -r $1'

# git-add
alias ga='git add'
alias gap='git add --patch'
alias .c='git add . --all && git commit -v'

# git-show
alias gow='git show'

# git-branch
alias gbD='git branch -D'
alias gb='git branch -v'

# git-blame
alias gB='git blame'

# git-diff
alias gd='git diff'
alias gds='git diff --staged'

# git-checkout
alias gob='git checkout -b'
alias go='git checkout'

# git-commit
alias gc='git commit -v'
alias gam='git commit --amend --verbose'
alias gcf='git commit --fixup'

# git-clone
alias gcl='git clone'

# git-remote
alias gra='git remote add'
alias grr='git remote rm'
alias gr='git remote -v'

# git-push
alias gp='git push'
alias gfu='git push -f'
alias gpo='git push origin master'
alias gPu='git push upstream master'
alias gpb='git push upstream $(git symbolic-ref --short -q HEAD)'
alias gpbd='git push upstream $(git symbolic-ref --short -q HEAD) --delete'
alias gpfb='git push -f upstream $(git symbolic-ref --short -q HEAD)'

# git-pull
alias guo='git pull upstream master --tags && git push origin master --tags'
alias gpl='git pull --rebase'

# git-log
alias gl='git log'
alias gld='git log -p'

# git-cherry-pick
alias gcp='git cherry-pick'

# git-stash
alias gst='git stash'
alias gsa='git stash apply'
alias gsd='git stash drop'

# tmux
alias tmux='tmux -2'
alias ta='tmux attach -t'
alias tls='tmux ls'
alias tkill='tmux kill-session -t'
alias th='tnew hack'

# tmuxinator
alias tx='tmuxinator'

# django
alias dmg='python manage.py migrate'
alias dmm='python manage.py makemigrations'
alias drs='python manage.py runserver'
alias dcs='python manage.py createsuperuser'
alias dsp='python manage.py shell_plus'
alias dch='python manage.py check'
alias drd='python manage.py reset_db'

# flask
alias fr='flask run'

# pytest
alias pt='pytest'
alias pk='pytest -sk'
alias ptx='pytest -x'
alias ptq='pytest -q'
alias ptw='ptw -- --testmon'

# stackage
alias sbs='ssh stackage-build'
alias sck='stack exec --resolver nightly -- stackage-curator check'

# pyenv
alias pyv='pyenv virtualenvs'
alias pya='pyenv activate'
alias pys='pyenv shell'

# jekyll
alias js='jekyll clean && jekyll serve --incremental'

# dict
alias dg='dict -d gcide'
alias dt='dict -d moby-thesaurus'
alias des='dict -d fd-eng-spa'
alias dse='dict -d fd-spa-eng'

# rainbowstream
alias rbs='rainbowstream'

# powertop
alias pwt='sudo powertop --auto-tune && sudo powertop'

# gtimelog
alias ge='$EDITOR ~/.gtimelog/timelog.txt'

# youtube-dl
alias ytl='youtube-dl'
alias ytx='youtube-dl -x'

# syncthing
alias st='sudo syncthing'

# iftop
alias ift='sudo iftop'

# docker
alias drn='docker run -it'

# pdf
alias pdf='evince'

# newsbeuter
alias nb='newsbeuter'

# eog
alias img='eog'

# pipenv
alias pe='pipenv'

# pip-tools
alias pc='pip-compile'
alias pup='pip install -U pip'

# ansible
alias ap='ansible-playbook'
alias ad='ansible-doc'

# hub
alias hpr='hub pull-request'
alias hcl='hub clone'

# fonts
alias newf='sudo fc-cache -vf'

# docker-compose
alias dcp='docker-compose'

# mutt
alias mutt='mutt -f pop://lukewm@mail.riseup.net'
